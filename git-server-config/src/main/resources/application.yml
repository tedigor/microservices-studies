server:
  port: 9080

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/HelserBarbosa/microservice-example
          
          